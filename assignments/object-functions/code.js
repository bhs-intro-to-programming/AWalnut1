const getX = (object) => {
  return object.x
};

const point = (x, y) => {
  let object = { x, y }
  return object
};

const emptyObject = () => {
  let object = {}
  return object
};

const distance = (obj1, obj2) => {
  return
};