const emit = (a, b) => console.log(a + ', ' + b);

const yesIfEven = (n) => {
  if (n % 2 === 0) {
    return 'yes'
  } return 'no'
};

const countXs = (s) => {
  let xs = 0
  for (let i = 0; i < s.length; i++) {
    if (s[i] === 'x') {
      xs++
    }
  }
  return xs
};

const timesTable = (n) => {
  return
};

const containsX = (s) => {
  for (let l = 0; l < s.length; l++) {
    console.log(l)
    if (s[l] === 'x') {
      return true
    }
    return false
  }
};

const sumSquares = () => {
  return
};